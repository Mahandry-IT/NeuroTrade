# ══════════════════════════════════════════════
# Trading Bot IA — Backend — Dependencies
# ══════════════════════════════════════════════

# ── Web framework ──
fastapi>=0.115,<1.0
uvicorn[standard]>=0.30,<1.0
python-multipart>=0.0.9

# ── Database ──
sqlalchemy>=2.0,<3.0
alembic>=1.13,<2.0
psycopg2-binary>=2.9,<3.0
python-dotenv>=1.0

# ── Validation / Settings ──
pydantic>=2.0,<3.0
pydantic-settings>=2.0,<3.0
email-validator>=2.0

# ── Security ──
python-jose[cryptography]>=3.3,<4.0
passlib[bcrypt]>=1.7,<2.0
bcrypt>=4.0,<4.1  # passlib 1.7.x incompatible with bcrypt 4.1+
cryptography>=42.0

# ── Scheduler ──
apscheduler>=3.10,<4.0

# ── AI — Gemini ──
google-genai>=1.0

# ── HTTP client ──
httpx>=0.27,<1.0

# ── Testing ──
pytest>=8.0,<9.0
pytest-asyncio>=0.23,<1.0
pytest-cov>=5.0
